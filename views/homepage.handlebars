{{#if logged_in}}
<h2>Welcome to Your Homepage</h2>

{{!-- Check if homepageData has friends and threads --}}
{{#if homepageData.friends.length}}
<h3>Your Friends' Threads</h3>
<ul>
  {{#each homepageData.friends as |friend|}}
  <li>
    {{#if friend.image}}
    <img src="{{friend.image}}" alt="{{friend.username}}'s Profile Image" class="profile-image">
    {{/if}}
    <h4>{{friend.username}}</h4>
    {{#if friend.threads.length}}
    <ul>
      {{#each friend.threads as |thread|}}
      <li>
        {{#if thread.author.image}} <!-- Assuming each thread has an author with a profileImage -->
        <img src="{{thread.author.image}}" alt="{{thread.author.username}}'s Profile Image" class="thread-author-image">
        {{/if}}
        <h5>{{thread.title}}</h5>
        <p>{{thread.content}}</p>
        <a href="/thread/{{thread.id}}">View Thread</a>
      </li>
      {{/each}}
    </ul>
    {{else}}
    <p>No threads from this friend.</p>
    {{/if}}
  </li>
  {{/each}}
</ul>
{{else}}
<p>You have no friends or your friends have no threads.</p>
{{/if}}

{{else}}
<p>You are not logged in.</p>
{{/if}}
